extends ../layout.pug

block content
  form.center-form#add-category-form(method='post', action="/category/add")
    h2 Add Category
    .form-group
      label(for='name') Name
      input#name.input-field(name='name', type='text')
    .form-group
      input#add-category-btn.btn(type='submit', value='Add')
